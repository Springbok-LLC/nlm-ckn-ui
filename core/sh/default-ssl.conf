<VirtualHost *:443>

    ServerName {subdomain}.{fqdn}
    ServerAdmin {server_admin}
    DocumentRoot /var/www/html

    SSLEngine on
    SSLCertificateFile      /etc/ssl/certs/certificate.pem
    SSLCertificateChainFile /etc/ssl/certs/certificate_chain.pem
    SSLCertificateKeyFile   /etc/ssl/private/decrypted_private_key.pem

    # Available log levels: trace8, ..., trace1, debug, info,
    # notice, warn, error, crit, alert, emerg.  LogLevel info
    # ssl:warn

    ErrorLog ${APACHE_LOG_DIR}/error-{subdomain}.log
    CustomLog ${APACHE_LOG_DIR}/access-{subdomain}.log combined

    Alias /static /home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}/react/build/static
    <Directory /home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}/react/build/static>
        Require all granted
    </Directory>

    <Directory /home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}/core>
        <Files wsgi.py>
        Require all granted
        # AuthType Basic
        # AuthName "Restricted Content"
        # AuthUserFile /etc/apache2/.htpasswd
        # Require valid-user
        </Files>
    </Directory>

    WSGIDaemonProcess {subdomain}.{fqdn}-ssl \
                      python-home=/home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}/.venv \
		      python-path=/home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}
    WSGIProcessGroup {subdomain}.{fqdn}-ssl
    WSGIScriptAlias / /home/ubuntu/nlm-ckn-ui-{nlm_ckn_ui_version}-{subdomain}/core/wsgi.py

</VirtualHost>
